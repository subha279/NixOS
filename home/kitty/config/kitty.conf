# ╭──────────────────────────────────────────────────────────────╮
# │ Kitty — Power User Configuration                            │
# │ NixOS • Hyprland • Zsh • Neovim • Wallust                  │
# ╰──────────────────────────────────────────────────────────────╯

# ─────────────────────────────────────────────────────────────
# THEME — static fallback
# Wallust is loaded LAST so its generated palette overrides these
# colors automatically whenever ~/.cache/wallust/kitty.conf exists.
# ─────────────────────────────────────────────────────────────

foreground            #e6e0e9
background            #141218
cursor                #e6e0e9
cursor_text_color     #141218

selection_foreground  #381e72
selection_background  #ccc2dc
url_color             #d0bcff

color0   #141218
color1   #ff728f
color2   #7fff9a
color3   #ffda72
color4   #bca5f2
color5   #4e3d76
color6   #d0bcff
color7   #f4efff
color8   #9d99a5
color9   #ff9fb2
color10  #a5ffb8
color11  #ffe7a5
color12  #d7c6ff
color13  #ded0ff
color14  #e9e0ff
color15  #faf8ff

# ─────────────────────────────────────────────────────────────
# FONT
# ─────────────────────────────────────────────────────────────

font_family        JetBrains Mono Nerd Font
bold_font          auto
italic_font        auto
bold_italic_font   auto
font_size          12.5

disable_ligatures  never
adjust_line_height  100%
adjust_column_width 0

# ─────────────────────────────────────────────────────────────
# WINDOW — translucent macOS-like surface
# ─────────────────────────────────────────────────────────────

remember_window_size      yes
remember_window_position  no

initial_window_width      1100
initial_window_height     700

window_padding_width      14
window_padding_height     10

window_border_width       0pt
draw_minimal_borders      yes

hide_window_decorations   yes

background_opacity        0.88
background_blur            32
dynamic_background_opacity yes

# Keep the terminal readable while allowing the environment behind it
# to remain visible through the translucent surface.
transparent_background_colors #141218

# ─────────────────────────────────────────────────────────────
# CURSOR
# ─────────────────────────────────────────────────────────────

cursor_shape            block
cursor_shape_unfocused  hollow
cursor_blink_interval   0.5
cursor_stop_blinking_after 15.0

# ─────────────────────────────────────────────────────────────
# SHELL / EDITOR
# ─────────────────────────────────────────────────────────────

shell               .
editor              .
shell_integration   enabled

# Better shell behavior/security.
allow_hyperlinks     yes
paste_actions        quote-urls-at-prompt,confirm
notify_on_cmd_finish invisible 10

# ─────────────────────────────────────────────────────────────
# SCROLLBACK
# ─────────────────────────────────────────────────────────────

scrollback_lines               50000
scrollback_pager_history_size  64
scrollbar_style                none
scrollback_fill_enlarged_window yes

# ─────────────────────────────────────────────────────────────
# TABS
# ─────────────────────────────────────────────────────────────

tab_bar_edge          top
tab_bar_style         powerline
tab_powerline_style   slanted
tab_bar_align         left
tab_bar_min_tabs      2

tab_bar_margin_width  0.0
tab_bar_margin_height 2.5 1.5

tab_bar_background    #141218

active_tab_foreground   #381e72
active_tab_background   #d0bcff
active_tab_font_style   bold

inactive_tab_foreground #cac4cf
inactive_tab_background #141218
inactive_tab_font_style normal

tab_activity_symbol " ● "

tab_title_template "{fmt.fg.red}{bell_symbol}{activity_symbol}{fmt.fg.tab}{title[:32]}{title[32:] and '…'}  [{index}]"
active_tab_title_template "{fmt.fg.red}{bell_symbol}{activity_symbol}{fmt.fg.tab}{title[:32]}{title[32:] and '…'}  [{index}]"

# ─────────────────────────────────────────────────────────────
# PERFORMANCE
# ─────────────────────────────────────────────────────────────

repaint_delay      10
input_delay        3
sync_to_monitor    yes
wayland_enable_ime yes

# Do not waste resources polling for updates.
update_check_interval 0

# ─────────────────────────────────────────────────────────────
# MOUSE / SELECTION
# ─────────────────────────────────────────────────────────────

mouse_hide_wait                  3.0
copy_on_select                   no
clear_selection_on_clipboard_loss yes

url_style          curly
open_url_with      default
allow_hyperlinks   yes

# ─────────────────────────────────────────────────────────────
# BELL
# ─────────────────────────────────────────────────────────────

enable_audio_bell   no
visual_bell_duration 0.0

# ─────────────────────────────────────────────────────────────
# SECURITY / REMOTE CONTROL
# ─────────────────────────────────────────────────────────────

# Local Unix socket only. No TCP/network remote control.
allow_remote_control socket-only
listen_on unix:${XDG_RUNTIME_DIR}/kitty-{kitty_pid}

# Programs cannot silently clone Kitty windows.
allow_cloning ask

# ─────────────────────────────────────────────────────────────
# LAYOUTS
# ─────────────────────────────────────────────────────────────

enabled_layouts *

# ─────────────────────────────────────────────────────────────
# POWER-USER KEYBINDS
# Kitty's default modifier remains Ctrl+Shift.
# ─────────────────────────────────────────────────────────────

# Windows
map kitty_mod+enter       new_window_with_cwd
map kitty_mod+n           new_window_with_cwd
map kitty_mod+w           close_window
map kitty_mod+f           toggle_fullscreen
map kitty_mod+z           toggle_layout stack
map kitty_mod+left        neighboring_window left
map kitty_mod+right       neighboring_window right
map kitty_mod+up          neighboring_window up
map kitty_mod+down        neighboring_window down

# Tabs
map kitty_mod+t           new_tab_with_cwd
map kitty_mod+shift+left  previous_tab
map kitty_mod+shift+right next_tab
map kitty_mod+shift+w    close_tab
map kitty_mod+shift+1    goto_tab 1
map kitty_mod+shift+2    goto_tab 2
map kitty_mod+shift+3    goto_tab 3
map kitty_mod+shift+4    goto_tab 4
map kitty_mod+shift+5    goto_tab 5
map kitty_mod+shift+6    goto_tab 6
map kitty_mod+shift+7    goto_tab 7
map kitty_mod+shift+8    goto_tab 8
map kitty_mod+shift+9    goto_tab 9

# Scrollback / shell integration
map kitty_mod+z          scroll_to_prompt -1
map kitty_mod+x          scroll_to_prompt 1
map kitty_mod+g          show_last_command_output
map kitty_mod+h          show_scrollback
map kitty_mod+slash      search_scrollback

# Command palette / control
map kitty_mod+f3         kitty_shell window
map kitty_mod+f4         debug_config
map kitty_mod+f5         load_config_file

# Hints — URLs / paths / lines
map kitty_mod+e          kitten hints --type=url --program=default
map kitty_mod+p>f        kitten hints --type=path
map kitty_mod+p>n        kitten hints --type=linenum --linenum-action=tab nvim +{line} {path}
map kitty_mod+p>y        kitten hints --type=hyperlink --program=default

# Unicode
map kitty_mod+u          kitten unicode_input

# Copy / paste
map --allow-fallback=shifted,ascii kitty_mod+c copy_to_clipboard
map --allow-fallback=shifted,ascii kitty_mod+v paste_from_clipboard
map kitty_mod+shift+c    copy_or_interrupt

# Font scaling
map kitty_mod+equal      change_font_size all +1.0
map kitty_mod+minus      change_font_size all -1.0
map kitty_mod+backspace  change_font_size all 0

# ─────────────────────────────────────────────────────────────
# WALLUST — dynamic palette
# IMPORTANT: keep this LAST.
# If Wallust has generated ~/.cache/wallust/kitty.conf, its colors
# override the static fallback palette above.
# ─────────────────────────────────────────────────────────────

include ~/.cache/wallust/kitty.conf

